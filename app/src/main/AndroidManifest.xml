<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.edmundconnor.clubemmobile">

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/clublogo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".NavigationActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.Dark"
            android:screenOrientation="portrait" ></activity>
        <activity android:name=".SettingsActivity" android:theme="@style/AppTheme.Dark" />
        <activity android:name=".ProfileActivity" android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.Dark"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" android:theme="@style/AppTheme.Dark" />
        <activity android:name=".CreateClubActivity" android:noHistory="true" android:theme="@style/AppTheme.Dark"/>
        <activity android:name=".CreateEventActivity" android:theme="@style/AppTheme.Dark" />
        <activity android:name=".ShowProfile" android:theme="@style/AppTheme.Dark"></activity>
        <activity android:name=".PrivateClubActivity" />
        <activity android:name=".PublicClubActivity" />
        <activity android:name=".Event2Activity"></activity>
        <activity android:name=".EventActivity"></activity><!-- ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information. -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>